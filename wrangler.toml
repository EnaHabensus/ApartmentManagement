name = "apartman"
compatibility_date = "2024-12-18"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./dist"

[vars]
# Ove varijable se overridaju u Cloudflare dashboardu za produkciju
# Lokalno se čitaju iz .dev.vars

[[kv_namespaces]]
binding = "SESSION"
id = "d281506e9e5846ebafe51c3fe0c75db8"

# Cron se pokreće external HTTP pozivom (npr. cron-job.org ili Cloudflare Worker)
# koji svaki dan u 8:00 UTC poziva: GET /api/cron/daily
# s headerom: x-cron-secret: <CRON_SECRET>
